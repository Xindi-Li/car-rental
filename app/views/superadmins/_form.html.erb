<%= form_for(:superadmin, url: superadmins_path) do |f| %>
    <% if @superadmin.errors.any? %>
        <div id="error_explanation">
          <h2 class="text-left"><%= pluralize(@superadmin.errors.count, "error") %> prohibited this superadmin from
            being saved:</h2>
          <ul>
            <% @superadmin.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :email %>
    <%= f.email_field :email %>

    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.label :password_confirmation, "Confirmation" %>
    <%= f.password_field :password_confirmation %>

    <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>